fastapi>=0.110
uvicorn[standard]>=0.27
python-docx>=1.1
pypdf>=4.0
requests>=2.31
python-multipart>=0.0.9
pydantic>=2.0

# Optional: OCR fallback for scanned/image PDFs (FR-1). Also requires the
# system binaries `tesseract` (Tesseract OCR) and `poppler` (pdftoppm).
#   macOS:   brew install tesseract poppler
#   Ubuntu:  sudo apt-get install tesseract-ocr poppler-utils
#   Windows: install Tesseract (UB-Mannheim build) + poppler, add both to PATH
# Disable with VERBATIM_OCR=0.
pytesseract>=0.3.10
pdf2image>=1.17
