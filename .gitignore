# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / frontend build
frontend/node_modules/
frontend/.vite/
# NOTE: frontend/dist/ is intentionally committed so the backend can serve the
# UI with zero npm setup (see README "Running it"). Rebuild with `npm run build`.

# Local run records (regenerated; not source)
data/runs/*.json

# OS
.DS_Store
